/**
 * Editor entry for the Withdrawal Request Form block.
 *
 * Styles come from the shared `sceu-withdrawal-form` handle (registered by
 * PublicForm and referenced in block.json), so there is no stylesheet to bundle
 * here — this entry just registers the block.
 */
import './editor.js';
