/**
 * Editor entry for the Right of Withdrawal block.
 *
 * Wires the editor stylesheet (extracted to index.css by the build) and the
 * block registration. The front-end stylesheet is `style.scss`, auto-detected
 * by @wordpress/scripts and emitted as style-index.css.
 */
import './editor.scss';
import './editor.js';
